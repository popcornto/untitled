public class quick {
/*

    public static void quicksortsmallesttobiggest(int[] a, int start, int end) {
        if (start < end) {
            int pivot = partitionsmallesttobiggest(a, start, end);
            quicksortsmallesttobiggest(a, start, pivot - 1);
            quicksortsmallesttobiggest(a, pivot + 1, end);
        }
    }

    public static int partitionbiggesttosmallest(int[] a, int start, int end) {
        int pivot = a[end];
        int i = start - 1;
        for (int j = start; j < end; j++) {
            if (a[j] <= pivot) {
                i++;
                int temp = a[i];
                a[i] = a[j];
                a[j] = temp;
            }
        }
        int temp = a[i + 1];
        a[i + 1] = a[end];
        a[end] = temp;
        return i + 1;
    }

    public static int partitionsmallesttobiggest(int[] a, int start, int end) {
        int pivot = a[end];
        int i = start - 1;
}*/
}
